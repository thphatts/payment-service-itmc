server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_itmc_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

club:
  webhook:
    secret: "CLB_SECRET_TOKEN_2026"
  treasurer:
    bank-id: "BIDV"
    account-no: "8817 3951 49"
    account-name: "NGUYEN THANH PHAT"